{
  "name": "hopeindexai",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "bun --watch server.ts",
    "start": "bun server.ts",
    "sync": "bun scripts/sync.ts",
    "enrich": "bun pipeline/enrich.ts",
    "train": "bun pipeline/train.ts",
    "ontology:validate": "bun scripts/validate_ontology_phase1.ts",
    "test:vercel": "bun test-handler.mjs"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
